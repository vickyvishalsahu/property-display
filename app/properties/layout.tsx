import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Property creation' }

const PropertiesLayout = ({ children }: { children: React.ReactNode }) => <>{children}</>

export default PropertiesLayout
