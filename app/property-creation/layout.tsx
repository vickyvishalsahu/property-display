import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Property creation' }

const PropertyCreationLayout = ({ children }: { children: React.ReactNode }) => <>{children}</>

export default PropertyCreationLayout
